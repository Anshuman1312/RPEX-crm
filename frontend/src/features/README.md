# Features Directory

Business modules are isolated under `src/features` and each module uses the same internal structure.

Required module subfolders:

- components
- pages
- hooks
- services
- types
- validation
- constants
- store

Additions to this contract should be discussed and documented in `src/app/architecture/README.md`.
